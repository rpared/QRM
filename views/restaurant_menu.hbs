    <div class="sidebar">
        <a href="/user_dashboard">User Dashboard</a>
        <a href="#">Restaurant Profile</a>
        <a href="#">Edit Account</a>
        <a href="#">Delete Account</a>
        <a href="#">Logout</a>
    </div>
    <main class="main-content">
        <div class="container">
            <h1>{{restaurant.resta_name}}</h1>
            <p>{{restaurant.resta_description}}</p>
             
            <h2>Menu</h2>
            <div class="menu-items">
                {{#each menuItems}}
                    <div class="menu-item">
                        <h3>{{this.item_name}} </h3>
                        <p>{{this.item_description}}</p>
                        <p>Price: ${{this.item_price}}</p>
                        {{#if this.item_photo}}
                        <img src="/uploads/{{this.item_photo}}" alt="{{this.item_name}}">
                        <p>Category: {{this.item_category}} / Labels: {{this.item_labels}}</p>
                        {{/if}}
                        <!-- Edit Button -->
                        <form action="/restaurant/{{../restaurant._id}}/menu/{{this._id}}/edit" method="GET" class="menu-item-form">
                        <button type="submit" class="btn btn-secondary">Edit</button>
                        </form>

                        <!-- Delete Button -->
                        <form action="/restaurant/{{../restaurant._id}}/menu/{{this._id}}/delete" method="POST" class="menu-item-form">
                        <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                {{/each}}
                
             </div>
            <!--
            <div class="menu-items">
                
                Menu items will be dynamically inserted here 
            </div>-->
            <form action="/add_menu_items" method="GET">
                        <button type="submit" class="btn btn-primary d-block">Add Menu Items</button>
            </form>
        </div>
    </main>

  <!-- No longer needed 
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const restaurantId = "{{restaurant._id}}"; // Pass restaurant ID to JS
            fetch(`/api/restaurant/${restaurantId}/menu_items`)
                .then(response => response.json())
                .then(menuItems => {
                    const menuItemsContainer = document.querySelector(".menu-items");
                    menuItems.forEach(item => {
                        const menuItemDiv = document.createElement("div");
                        menuItemDiv.classList.add("menu-item");
                        menuItemDiv.innerHTML = `
                            <h3>${item.item_name}</h3>
                            <p>${item.item_description}</p>
                            <p>Price: $${item.item_price}</p>
                            ${item.item_photo ? `<img src="/uploads/${item.item_photo}" alt="${item.item_name}">` : ""}
                        `;
                        menuItemsContainer.appendChild(menuItemDiv);
                    });
                })
                .catch(error => {
                    console.error("Error fetching menu items:", error);
                });
        });
    </script>

-->
